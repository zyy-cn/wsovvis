WS-OVVIS acceptance evaluation
Timestamp: 20260311T040711Z
Gate evaluated: G0
Acceptance judgment: not satisfied
Evidence judgment: partial but blocked
Final status: BLOCKED

Acceptance items satisfied:
- the active-gate interpretation is unambiguous under the deployed docs: `AGENTS.md`, `docs/mainline/*`, the supervisor helper dry-run, and the runbook all resolve the current loop to `G0`
- the authority switch to `docs/mainline/*` is established locally rather than `docs/mainline_v9/*` or deleted legacy docs
- `docs/mainline/CODEBASE_MAP.md` matches the current repository anchors and correctly leaves the planned v9 entrypoints absent
- `docs/mainline/ENVIRONMENT_AND_VALIDATION.md` matches the real repository tooling and git facts that are locally inspectable: wrapper/checker presence, origin fetch URL, and push-route URL

Acceptance items not yet satisfied:
- canonical environment inheritance has still not been recorded from the approved remote path; the exact retry on `20260311T040711Z` reached `gpu4090d` but failed before the verification command ran
- `remote HEAD == intended local commit` has still not been recorded; the exact retry on `20260311T040711Z` failed because `origin/mainline-v9-g0` was not available for checkout
- `docs/mainline/STAGEB_INTERFACE_CONTRACT.md` still needs a doc-only reconciliation to the current Stage B export artifact contract, but that step remains intentionally deferred until canonical replay is recorded

Evidence items satisfied:
- control-plane dry-run readout proving `docs/mainline/*` is the active interpretation source
- authority-chain comparison against deleted draft `docs/mainline_v9/*` control docs
- one worked example showing how `G0` is resolved from the docs

Evidence items missing or blocked:
- canonical runner bootstrap evidence from `/home/zyy/code/wsovvis_runner`
- remote wrapper output that records the inherited conda recipe and `PYTHONPATH`
- remote `HEAD` for the intended local commit
- the deferred G0 doc-only reconciliation outcome for `docs/mainline/STAGEB_INTERFACE_CONTRACT.md`

Next gate activation allowed: no
Fallback path:
- keep the repository in `G0`
- first restore canonical connectivity and record the blocked inheritance items
- then close the remaining Stage B contract wording gap without widening scope
