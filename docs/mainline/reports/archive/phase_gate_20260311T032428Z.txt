WS-OVVIS phase/gate check
Timestamp: 20260311T032428Z
Status: BLOCKED
Active gate: G0 — v9 control-plane bootstrap and inheritance verification

Why this gate is active:
- `AGENTS.md`, `docs/mainline/INDEX.md`, `docs/mainline/PLAN.md`, `docs/mainline/STATUS.md`, and `docs/runbooks/mainline_phase_gate_runbook.md` all resolve the current bounded loop to `G0`
- `python tools/run_mainline_loop.py --dry-run` printed `authoritative current gate from STATUS.md: \`G0\``
- `docs/mainline/EXECUTION_SOURCE.md` declares `docs/mainline/*` as the execution source and explicitly replaces draft `docs/mainline_v9/*` packs

Blocking acceptance condition:
- canonical replay could not run from this environment: `bash tools/remote_verify_wsovvis.sh ...` failed with `ssh: connect to host 172.29.112.1 port 2222: No route to host`
- intended-commit remote reachability could not be recorded: `git ls-remote origin refs/heads/mainline-v9-g0` failed with `ssh: Could not resolve hostname github.com: Temporary failure in name resolution`
- `docs/mainline/STAGEB_INTERFACE_CONTRACT.md` does not yet cleanly match the current Stage B export artifact fields; the exporter writes `track_id`, `row_index`, temporal support, `objectness_score`, embeddings, JSON manifests, and NPZ arrays, while the contract currently states `local_track_id`, mask or mask-reference fields, and local-query feature fields as required minima

Smallest next valid step:
- restore SSH and network reachability to `gpu4090d` and `github.com`
- rerun the same bounded `G0` wrapper command to record remote bootstrap, env inheritance, and `remote HEAD == intended local commit`
- if remote replay becomes available, make only the minimal G0 doc reconciliation needed to align `STAGEB_INTERFACE_CONTRACT.md` with the current `track_id`-based export or explicitly mark the missing fields as planned v9 additions

Out of scope modules:
- algorithmic Stage B / Stage C / Stage D changes
- default-off modules listed in `AGENTS.md`
- any gate activation beyond `G0`

Fallback path if blocked or failed:
- use `docs/mainline/FAILURE_PLAYBOOK.md` G0 control-plane fallback order
- for environment inheritance, keep the gate `BLOCKED` until wrapper path, bootstrap preflight, push route, runner dir, and `remote HEAD == intended local commit` can be recorded
