WSOVVIS acceptance evaluation
Timestamp (UTC): 2026-03-08T11:09:31Z
Active gate evaluated: `G5 - Full-video linking and inference closure`
Branch: `codex/p3-stagec-c0-c4-semantic-slice`
Intended local commit: `5e9172b7f9f685f9409ebd8351b028272d00b7cc`

1. Evidence reviewed
- `PLAN.md`
- `METRICS_ACCEPTANCE.md`
- `FAILURE_PLAYBOOK.md`
- current `STATUS.md`
- current `CODEBASE_MAP.md`
- authoritative runtime files:
  `third_party/VNext/projects/SeqFormer/seqformer/models/clip_output.py`
  `third_party/VNext/projects/SeqFormer/seqformer/seqformer.py`
- canonical runtime smoke artifact:
  `codex/g5_linking_inference_20260308T110931Z/g5_linking_smoke_summary.json`
- remote canonical wrapper validation on `gpu4090d`
- remote HEAD consistency evidence

2. Judgment
- `FAIL`

3. Acceptance conditions satisfied
- the full-video closure runtime path is live under canonical remote validation
- the clip-matching path produced a merged linked result with `linked_track_count=1`
- the whole-video inference path produced track ids and embeddings
- remote `HEAD` equals the intended local commit

4. Acceptance conditions still missing
- direct bounded evidence that the accepted G4 attribution path remains stable after G5 closure on the documented defaults
- dedicated repo-level unit test coverage for the active G5 runtime surfaces

5. Acceptance conditions failed
- the active linking path does not implement the documented bounded `geometry + query` primary linking policy; `Videos.update` matches by mask sIoU only
- the active global classification closure does not implement the documented bounded `quality-weighted logit averaging` default; `Videos.get_result` performs uniform averaging over valid clips
- because those default-path mismatches are present in the authoritative runtime, G5 acceptance is not satisfied

6. May the next gate activate?
- `no`
- `G5` must remain active until the bounded linking and classification defaults match the authoritative policy and the same canonical smoke passes on that aligned path

7. Fallback path
- Use `FAILURE_PLAYBOOK.md` for G5:
  1. reduce semantics in the linking score,
  2. use geometry + query as primary linking signals,
  3. revert global classification to quality-weighted logit averaging,
  4. keep stronger memory aggregation off.

8. What should be written back to `STATUS.md`
- `Active gate: G5 - Full-video linking and inference closure`
- `Current G5 judgment: FAIL`
- authoritative G5 linking / inference / stitching files
- note that dedicated repo-level G5 unit tests are currently absent
- record the canonical runtime smoke artifact path and remote HEAD consistency
- record the blocking bounded-policy mismatches:
  mask-sIoU-only linking and uniform clip averaging for class closure
- `Next smallest valid step`: one bounded G5 implementation loop to restore policy alignment, then rerun the same canonical smoke
